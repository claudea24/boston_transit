{
  "name": "weather",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "packageManager": "npm@11.6.2",
  "devDependencies": {
    "@playwright/mcp": "^0.0.53",
    "turbo": "^2"
  }
}
