[build]
buildCommand = "npm run build --workspace=packages/shared && npm run build --workspace=apps/worker"

[deploy]
startCommand = "npm run start --workspace=apps/worker"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
